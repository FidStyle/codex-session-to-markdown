[package]
name = "codex-session-to-markdown"
version = "0.1.0"
edition = "2021"
description = "Convert Codex session JSONL files to Markdown prompt transcripts."
license = "MIT"

[dependencies]
serde_json = "1"
